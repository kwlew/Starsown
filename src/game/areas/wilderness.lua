--- The peaceful ring around the Hub -- open ground, but still safe
-- (noSpawn), so leaving the plaza on foot doesn't mean immediate danger.
-- No NPCs of its own yet; nothing else to do here but walk through it on
-- the way to the Wastes.
return {
    id = "wilderness",
    radius = { min = 300, max = 700 },
    noSpawn = true,
}
