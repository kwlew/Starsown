--- Placeholder Hub vendor -- no shop system exists yet (see PLAN.md §5.4),
-- so `shop` is inert data for now, same as `interaction`.
return {
    id = "blacksmith", sides = 4, radius = 14, color = "metal",
    interaction = "shop", shop = "blacksmithWares",
}
