--- The Wastes' own resident -- not every NPC belongs in the Hub (see
-- game/areas/wastes.lua's `npcs` list). Flavor only, same shape as the
-- wanderer.
return { id = "scout", sides = 6, radius = 13, color = "plant", interaction = "dialogue", dialogue = "scout" }
